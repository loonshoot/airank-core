{
  "name": "outrun-core",
  "private": true,
  "workspaces": [
    "batcher",
    "config",
    "listener",
    "mcp"
  ],
  "version": "1.0.0",
  "scripts": {
    "dev": "node scripts/start-dev.js",
    "dev:legacy": "concurrently -n ngrok,mongo,api,batcher,graphql,listener,stream,mcp -c \"white,red,cyan,green,yellow,blue,magenta,orange\" \"node scripts/ngrok-tunnel.js\" \"docker compose up -d mongo1 || true\" \"cd api-gateway && npm run dev\" \"cd batcher && npm run dev\" \"cd graphql && npm run dev\" \"cd listener && npm run dev\" \"cd stream && npm run dev\" \"cd mcp && npm run dev\"",
    "dev:down": "docker compose down",
    "dev:cleanup": "node scripts/cleanup.js",
    "ngrok": "node scripts/ngrok-tunnel.js",
    "test:webhook:salesforce": "node scripts/test-salesforce-webhook.js",
    "setup:ngrok": "node scripts/setup-ngrok.js"
  },
  "devDependencies": {
    "axios": "^1.6.2",
    "concurrently": "^8.2.2",
    "dotenv": "^16.5.0",
    "ngrok": "^5.0.0-beta.2"
  },
  "dependencies": {
    "@hokify/agenda": "^6.3.0",
    "agenda": "^5.0.0",
    "mongoose": "^8.13.2",
    "redis": "^4.6.7",
    "uuid": "^11.1.0"
  }
}
