{
  "name": "airank-listener",
  "version": "1.0.0",
  "description": "MongoDB change stream listener for AIRank batch processing",
  "main": "index.js",
  "scripts": {
    "start": "bun run index.js",
    "dev": "bun --watch run index.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "mongodb": "^6.0.0",
    "mongoose": "^8.0.0",
    "agenda": "^5.0.0"
  }
}
